{
  "name": "rivals-clone",
  "version": "1.1.1",
  "private": true,
  "description": "RIVALS-style browser duel — AI + online multiplayer",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R css js dist/",
    "build:vercel": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
